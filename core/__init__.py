"""Core logic package: system access, vision, actions, control flow.

Nothing in this package depends on a UI toolkit widget (bot_engine imports
QtCore only, never QtWidgets), so the whole package can be tested -- or driven
from a command line front end -- without a graphical interface.
"""
